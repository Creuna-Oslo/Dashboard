language: node_js
node_js: "lts/*"
cache: yarn
install:
  - cd app && yarn
script:
  - yarn test
deploy:
  provider: firebase
  token: $FIREBASE_TOKEN
  project: creuna-dashboard
  on:
    branch: master
  skip_cleanup: true